services:
  - type: web
    name: streamlit-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
